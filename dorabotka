#include <iostream>
#include <fstream>
#include <stdio.h>

using namespace std;

 class Verticals{
     
 };
 class normali{
     
 };
 class tex_cord{
     
 };
 class facels{
     
 };
 
 void file_tok(string buffer){
     ifstream f("cube.obj");
     while(!f.eof()){
        char *fgets(buffer, 1, f);
     }
 }
int main()
{
    fopen("cube.obj","r");
    string buffer;
    double x, y, z;

    return 0;
}
