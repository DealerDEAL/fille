#include <iostream>
#include <fstream>
#include <stdio.h>

using namespace std;

 class Verticals{
    double x, y, z;
 };
 class normali{
     double a, b, c;
 };
 class tex_cord{
     double x, y;
 };
 class facels{
     
 };

int main()
{
    ifstream file; // создаем объект класса ifstream
    file.open("cube.obj"); // открываем файл
    char bufer[256];
    fgets(bufer, sizeof(bufer), file);

    return 0;
}
